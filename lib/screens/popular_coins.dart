import 'package:flutter/material.dart';

class PopularCoinsScreen extends StatelessWidget {
  const PopularCoinsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    throw UnimplementedError();
  }
}
